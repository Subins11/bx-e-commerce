import { BxCurrencyPipe } from './bx-currency.pipe';

describe('BxCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new BxCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
