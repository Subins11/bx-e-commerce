import { BxTransitionDirective } from './bx-transition.directive';

describe('BxTransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new BxTransitionDirective();
    expect(directive).toBeTruthy();
  });
});
